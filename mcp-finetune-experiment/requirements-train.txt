# Real incremental LoRA/QLoRA training (requires a GPU).
# Install with:  pip install -r requirements-train.txt
torch>=2.2
transformers>=4.44
peft>=0.12
trl>=0.10
accelerate>=0.33
datasets>=2.20
bitsandbytes>=0.43
